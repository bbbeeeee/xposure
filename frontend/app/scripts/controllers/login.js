'use strict';

/**
 * @ngdoc function
 * @name exposeApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the exposeApp
 */
angular.module('exposureApp')
  .controller('LoginCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
